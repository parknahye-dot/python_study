## 11/7 수업내용

# 🎨 Matplotlib 시각화 Part 1

## 1️⃣ Matplotlib 기본 설정

| 메소드 / 명령어 | 설명 |
|:--|:--|
| `pip install matplotlib` | Matplotlib 설치 |
| `import matplotlib.pyplot as plt` | 그래프 모듈 불러오기 |
| `import numpy as np` | 수치 계산용 모듈 |
| `!apt-get -qq -y install fonts-nanum` | Colab에서 한글 폰트 설치 |
| `fm.findSystemFonts()` | 시스템 내 폰트 경로 찾기 |
| `plt.rcParams['font.family']` | 그래프에 적용할 폰트 설정 |
| `plt.rcParams['axes.unicode_minus'] = False` | 마이너스(-) 깨짐 방지 |

---

## 2️⃣ 기본 선 그래프 (Line Plot)

| 메소드 / 옵션 | 설명 |
|:--|:--|
| `plt.plot(x, y)` | 선 그래프 그리기 |
| `plt.xlabel('X축 이름')` | x축 레이블 설정 |
| `plt.ylabel('Y축 이름')` | y축 레이블 설정 |
| `plt.title('제목')` | 그래프 제목 지정 |
| `plt.grid(True)` | 격자 표시 |
| `plt.legend()` | 범례 표시 |
| `plt.figure(figsize=(w, h))` | 그래프 크기 지정 |
| `plt.xticks() / plt.yticks()` | 축 눈금 표시 |
| `plt.show()` | 그래프 출력 |

---

## 3️⃣ 선 스타일 및 색상 옵션

| 옵션명 | 설명 |
|:--|:--|
| `color='r'` | 선 색상 지정 (‘r’, ‘g’, ‘b’, ‘k’) |
| `linestyle='--'` | 선 스타일 (‘-’, ‘--’, ‘:’, ‘-.’) |
| `marker='o'` | 데이터 포인트 표시 (‘o’, ‘s’, ‘^’, ‘*’) |
| `linewidth=2` | 선 두께 지정 |
| `alpha=0.7` | 투명도 설정 |
| `label='데이터 이름'` | 범례용 이름 설정 |

---

## 4️⃣ 여러 선 그래프

| 메소드 | 설명 |
|:--|:--|
| `plt.plot(x, y1, label='제곱')` | 첫 번째 선 추가 |
| `plt.plot(x, y2, label='선형')` | 두 번째 선 추가 |
| `plt.legend()` | 여러 선에 대한 범례 표시 |
| `plt.title('두 함수 비교')` | 제목 지정 |
| `plt.grid(True)` | 격자 표시 |

---

## 5️⃣ 그래프 크기 / 시각적 조정

| 메소드 | 설명 |
|:--|:--|
| `plt.figure(figsize=(10, 6))` | 전체 그래프 크기 지정 |
| `plt.tight_layout()` | 여백 자동 조정 |
| `plt.axis([xmin, xmax, ymin, ymax])` | 축 범위 수동 지정 |
| `plt.xlim()` / `plt.ylim()` | 각 축 범위 지정 |

# 🎯 1.3 산점도 (Scatter Plot)

| 메소드 / 옵션 | 설명 |
|:--|:--|
| `plt.scatter(x, y)` | 산점도 기본 그래프 |
| `c='blue'` | 점 색상 지정 |
| `marker='o'` | 점 모양 지정 (‘o’, ‘s’, ‘^’, ‘x’ 등) |
| `s=100` | 점 크기 지정 |
| `alpha=0.6` | 투명도 설정 |
| `label='그룹명'` | 범례에 표시할 이름 |
| `plt.legend()` | 범례 표시 |
| `plt.grid(True, alpha=0.3)` | 격자 표시 |
| `plt.figure(figsize=(10,6))` | 그래프 크기 조정 |
| `plt.annotate(text, (x,y))` | 점 옆에 텍스트 표시 |

---

# 📊 1.4 막대 그래프 (Bar Plot)

| 메소드 / 옵션 | 설명 |
|:--|:--|
| `plt.bar(x, height)` | 세로 막대 그래프 |
| `plt.barh(y, width)` | 가로 막대 그래프 |
| `color='steelblue'` | 막대 색상 설정 |
| `edgecolor='black'` | 테두리 색상 설정 |
| `plt.xlabel()` / `plt.ylabel()` | 축 이름 지정 |
| `plt.title()` | 제목 지정 |
| `plt.xticks()` / `plt.yticks()` | 눈금 레이블 변경 |
| `plt.grid(axis='y', alpha=0.3)` | y축 방향 격자 표시 |
| `plt.tight_layout()` | 레이아웃 자동 정렬 |

---

## 그룹 막대 그래프 (Grouped Bar)

| 메소드 / 옵션 | 설명 |
|:--|:--|
| `np.arange(len(x))` | x축 인덱스 생성 |
| `width = 0.35` | 막대 간격 조정 |
| `plt.bar(x - width/2, data1)` | 왼쪽 막대 |
| `plt.bar(x + width/2, data2)` | 오른쪽 막대 |
| `plt.legend()` | 그룹별 범례 표시 |
| `ax.bar()` | 서브플롯 내 막대 그래프 |
| `ax.set_xticks()` / `ax.set_xticklabels()` | 눈금 이름 지정 |
| `fig, ax = plt.subplots()` | 개별 Axes 생성 (객체형 그래프) |

---

## 누적 막대 그래프 (Stacked Bar)

| 메소드 / 옵션 | 설명 |
|:--|:--|
| `plt.bar(x, a)` | 첫 번째 데이터 |
| `plt.bar(x, b, bottom=a)` | a 위에 b 누적 |
| `bottom=[a+b for a,b in zip()]` | 누적 기준 계산 |
| `plt.bar(..., color='#FF6B6B')` | 사용자 색상 코드 지정 |
| `plt.legend()` | 범례 표시 |
| `plt.title('누적 막대 그래프')` | 제목 지정 |
| `plt.tight_layout()` | 여백 조정 |

---

## 비율 막대 그래프

| 메소드 / 옵션 | 설명 |
|:--|:--|
| `plt.bar(..., bottom=이전비율)` | 누적 비율 그래프 생성 |
| `plt.ylabel('%')` | 축 단위 표시 |
| `np.mean()` | 평균 계산 |
| `plt.show()` | 그래프 출력 |

# 📈 1.5 히스토그램 (Histogram)

| 메소드 / 옵션 | 설명 |
|:--|:--|
| `plt.hist(data, bins=30)` | 데이터의 분포를 히스토그램으로 표시 |
| `bins` | 구간 개수 (기본 10) |
| `range=(min, max)` | 히스토그램 표시 범위 지정 |
| `color='skyblue'` | 막대 색상 지정 |
| `edgecolor='black'` | 막대 테두리 색상 지정 |
| `alpha=0.7` | 투명도 설정 |
| `density=True` | 빈도 대신 확률 밀도로 표시 |
| `plt.axvline(mean, color='r', linestyle='--')` | 평균선 표시 |
| `plt.title()` / `plt.xlabel()` / `plt.ylabel()` | 그래프 제목 및 축 이름 |
| `plt.tight_layout()` | 여백 자동 조정 |
| `plt.show()` | 그래프 출력 |

---

## 📊 다중 히스토그램 (Multiple Histogram)

| 메소드 / 옵션 | 설명 |
|:--|:--|
| `plt.hist(data1, alpha=0.5, label='A')` | 첫 번째 분포 |
| `plt.hist(data2, alpha=0.5, label='B')` | 두 번째 분포 |
| `plt.legend()` | 범례 표시 |
| `plt.subplot(1, 2, 1)` | 서브플롯 (1행 2열, 첫 번째 그래프) |
| `plt.subplot(1, 2, 2)` | 두 번째 그래프 |
| `plt.figure(figsize=(12, 6))` | 전체 그래프 크기 조정 |
| `np.random.normal(mean, std, n)` | 정규분포 데이터 생성 |
| `np.median()` / `np.std()` / `np.mean()` | 통계값 계산 |

---

## 🗺️ 2D 히스토그램 (2D Histogram)

| 메소드 / 옵션 | 설명 |
|:--|:--|
| `plt.hist2d(x, y, bins=30, cmap='YlOrRd')` | 2차원 데이터 분포 |
| `cmap` | 색상 맵 (heatmap 스타일) |
| `plt.colorbar(label='빈도')` | 색상 막대 추가 |
| `plt.scatter(x, y, alpha=0.3)` | 산점도와 비교용 그래프 |
| `np.corrcoef(x, y)[0, 1]` | 두 변수의 상관계수 계산 |
| `plt.tight_layout()` | 서브플롯 여백 정리 |

---

# 🥧 1.6 파이 차트 (Pie Chart)

| 메소드 / 옵션 | 설명 |
|:--|:--|
| `plt.pie(sizes)` | 기본 파이 차트 생성 |
| `labels=['A', 'B', 'C']` | 각 조각 이름 지정 |
| `autopct='%1.1f%%'` | 비율 표시 형식 지정 |
| `colors=['gold', 'lightcoral', 'skyblue']` | 사용자 색상 설정 |
| `explode=(0.1, 0, 0)` | 첫 번째 조각 분리 강조 |
| `shadow=True` | 그림자 효과 추가 |
| `startangle=90` | 시작 각도 지정 |
| `plt.title('비율 그래프')` | 제목 지정 |
| `plt.show()` | 그래프 출력 |

---

# 📘 통계 및 분석 관련 주요 메소드 (정리)

| 함수 | 설명 |
|:--|:--|
| `np.mean(data)` | 평균 계산 |
| `np.median(data)` | 중앙값 계산 |
| `np.std(data)` | 표준편차 계산 |
| `np.argmax(data)` | 최댓값의 인덱스 반환 |
| `np.corrcoef(x, y)` | 상관계수 계산 |
| `zip(a, b)` | 두 리스트 병렬 순회 (예: gap 계산) |
| `sum(list)` | 합계 계산 |
